import styled from 'styled-components';

export const NavBarContainer = styled.nav`
    display: flex;
    align-items: center;
    gap: 15px;

    li:nth-child(1) {
        position: relative;
        width: auto;
    }
`;